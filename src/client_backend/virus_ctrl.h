#pragma once
#ifndef VIRUS_CTRL_H
#define VIRUS_CTRL_H
#include <stdio.h>
#include <iostream>
int virus_ctrl_store(const char*, const char*,const char* id);
int virus_ctrl_process(const char* id);
#endif
